<h1>Add your restaurant</h1>
<%= simple_form_for(@restaurant) do |r| %>
  <%= r.input :name  %>
  <%= r.input :address  %>
  <%= r.input :phone_number %>
  <%= r.input :category %>
  <%= r.submit %>
<% end %>
<p><%= link_to "Cancel", restaurants_path %></p>
