<h1>All Restaurant</h1>
<div class="" style=" height: 600px; overflow-y: scroll">
  <% @restaurants.each do |restaurant| %>
    <p><%= link_to restaurant.name, restaurant_path(restaurant), :class =>"btn btn-secondary text-light text-capitalize btn-lg d-grid gap-2 shadow p-3" %></p> <br/>
  <% end %>
</div>
<p><%= link_to 'Add your restaurant', new_restaurant_path, :class =>"btn btn-primary text-light mt-3" %></p>
